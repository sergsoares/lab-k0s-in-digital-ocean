apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-ingress
  namespace: argo-cd
spec:
  entryPoints:
    - websecure
  routes:
  - kind: Rule
    match: Host(`argocd.lab-k0s.ourlab.cc`)
    services:
    - name: argo-cd-argocd-server
      port: 443